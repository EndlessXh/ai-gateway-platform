# OpenRouter direct-validation credential.
# Copy this file to deploy/.env.openrouter.local and set the value locally.
# The local file is ignored by Git and must never be committed or printed.
OPENROUTER_API_KEY=
