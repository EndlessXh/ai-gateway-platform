# Project-local Claude Code gateway configuration.
# Copy to deploy/.env.hyc-client.local (Git-ignored) and provide a short-lived,
# model-scoped HYC AI user token. Never put an OpenRouter key in this file.
HYC_API_BASE_URL=http://127.0.0.1:3001
HYC_API_TOKEN=
HYC_PUBLIC_MODEL=claude-opus-4.6
